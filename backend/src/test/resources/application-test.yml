server:
  port: 0  # 随机端口，避免冲突

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/emergency_auth?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: root

  data:
    redis:
      host: localhost
      port: 6379

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: test-secret-key-for-jwt-signing-should-be-long-enough
  expiration: 3600000
  header: Authorization
  prefix: Bearer

ai-service:
  base-url: http://localhost:8000
  timeout: 30000

data-pipeline:
  base-url: http://localhost:8000
  timeout: 30000

dify:
  base-url: http://localhost:8080
  api-key: app-ELApZzN6iN2LXRfEU2ckM62R
  workflow-id: natural-disaster-workflow
  dataset-api-key: dataset-sqapyDa5F43pjdaBdkwYn3VU

neo4j:
  uri: bolt://localhost:7687
  username: neo4j
  password: 12345678
  database: neo4j

logging:
  level:
    com.yunnan.emergency: debug
